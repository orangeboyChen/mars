// Top-level build file where you can add configuration options common to all
// sub-projects/modules.
//
// The Android Gradle Plugin version is declared once here and applied by the
// modules without a version, so every module of the build resolves the same
// plugin. Repositories live in settings.gradle.kts.

plugins {
    id("com.android.library") version "9.4.1" apply false
}
